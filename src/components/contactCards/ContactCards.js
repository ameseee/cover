import React from 'react';

const ContactCards = () => {

  return (
    <div>
      <h3>Contact Name</h3>
      <button>send custom text</button>
      <button>send my location NOW</button>
    </div>
  );
};

export default ContactCards;
